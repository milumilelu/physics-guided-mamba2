# v3 QA status

The existing montage and individual PNG files visualize the archived v2 run.
No v3 montage was generated because v3 stopped before resampling with 36 hard
four-edge failures. See `registration/v3_failure_audit.json`.
