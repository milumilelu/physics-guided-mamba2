# WP7 D4 人工确认

本图仅用于方向复核，不是 canonical `H_reg`。原始坐标定义为图像向右是 +X、向下是 +Y。

请针对每个 session 记录：canonical +y（长扫描方向）在原图中指向上/下/左/右，canonical +x（相邻扫描线/hatch 方向）在原图中指向何方，以及依据（显微镜方向、加工路径或实验记录）。不得用‘哪边更深’决定方向。

连续角度已经独立估计；D4 未确认前脚本不会输出 `H_reg`。
