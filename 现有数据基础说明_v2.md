# 现有实验与测量数据基础说明

> 版本：v1.1（当前状态修订）  
> 更新日期：2026-09-01  
> 适用范围：氧化锆矩形面加工、氧化锆单线设计及当前仓库中的共聚焦预处理结果。  
> 原则：区分设计记录、测量来源、ROI 样本和统计独立单位；区分已由仓库验证的事实与仅由实验背景资料确认的条件。

---

# 1. 当前数据总体结构

| 数据组 | 设计记录数 | 结构 | 当前数据状态 |
|---|---:|---|---|
| 120 正式矩形面加工 | 120 | 五因素空间填充 DOE | 已生成 120 个稳定 ROI |
| 60 pass 实验 | 60 | 15 条基础条件 × N=1–4 | 已生成 60 个稳定 ROI |
| 20 补充 pass 5–6 | 20 | 10 条基础条件 × N=5–6 | 已生成 20 个稳定 ROI |
| 氧化锆单线加工 | 120 | 四因素 DOE | 有设计表，尚未并入矩形统一数据集 |

设计记录总数：

$$120+60+20+120=320.$$

当前已经形成统一高度数据集的矩形记录数：

$$\boxed{120+60+20=200.}$$

设计记录数、ROI 数和独立测量来源数不是同一概念。

---

# 2. 当前正式矩形高度数据集

正式数据对象：

```text
physics-guided Mamba-2/
  outputs/rectangle_registration/manual_internal_roi_v1/dataset/
    stable_roi_80um_dataset.npz
    stable_roi_80um_index.csv
    dataset_summary.json
```

数据规模：

```text
samples:       200
shape:         200 × 160 × 160
physical ROI:  80 × 80 μm
pixel pitch:   0.5 μm/pixel
status:        200/200 PASS
```

NPZ 数组：

```text
height_raw[200,160,160]       float32，正式主数据
height_repaired[200,160,160]  float32，敏感性分析版本
valid_mask[200,160,160]       bool
repair_mask[200,160,160]      bool
session_id[200]
measurement_id[200]
sample_id[200]
x_um[160]
y_um[160]
```

`height_raw` 是正式证据，修复数据不得覆盖 raw。当前所有 bundle 像素均为 finite，正式 ROI 的 `valid_fraction=1.0`。

---

# 3. ROI 与测量来源的层级

200 个 ROI 来自 160 个 measurement：

| session | ROI 数 | measurement 数 | ROI/measurement |
|---|---:|---:|---:|
| `zro2_120_formal` | 120 | 120 | 1 |
| `zro2_60_pass` | 60 | 30 | 2 |
| `zro2_20_supplement` | 20 | 10 | 2 |

因此当前最少存在：

$$\text{measurement/shared height source}\rightarrow\text{ROI}$$

两层实验单位。

`annotations/sample_view_manifest.csv` 中的 `shared_height_source_id` 用于永久记录这一关系。后续 bootstrap、交叉验证和不确定性分析不得把同一共享高度来源中的两个 ROI 当作完全独立测量。

正式 ROI 身份键为：

```text
(session_id, sample_id)
```

`sample_id` 和 `measurement_id` 在不同 session 间会重复，不能单独作为全局主键。

---

# 4. 原始矩形共聚焦数据与解析状态

当前三个正式 CAG 文件：

```text
氧化锆/pass实验数据/120正式.cag
氧化锆/pass实验数据/60Pass组.cag
氧化锆/pass实验数据/20补充pass.cag
```

对应设计表：

```text
氧化锆/pass实验数据/120正式_design记录表_0516.csv
氧化锆/pass实验数据/60pass_design.csv
氧化锆/pass实验数据/20补充_pass5_6实验.csv
```

旧版说明中“尚无可靠 CAG 解析器、需等待批量导出 CSV”的状态已经过期。

当前仓库已经实现并验证 CAG 高度读取：

- 直接高度与容器内嵌 VK4 高度一致；
- 使用真实官方 CSV 行进行独立数值交叉检查；
- 明确处理 KEYENCE 三位小数 half-away-from-zero 舍入；
- 无效 sentinel 转换为 NaN，不进入统计；
- 真实容器结构、LUT 偏移和数据名称覆盖有自动测试。

当前测试集共 49 项，全部通过。CAG 可以作为当前正式预处理输入，不再要求先导出 200 份 CSV。

---

# 5. 当前预处理流程

当前冻结流程为：

```text
原始 CAG
→ measurement 外部未加工表面平面校正
→ session 固定方向
→ 200 个冻结人工矩形注册
→ 中央 80 × 80 μm 稳定 ROI
→ 保守二维锥坑修复及 repair mask
→ 200 × 160 × 160 数据包
```

关键配置与冻结输入：

```text
config/manual_internal_roi_v1.yaml
config/frozen/measurement_planes_160.csv
config/frozen/manual_registration_200.csv
annotations/manual_four_edge_validation_frozen.csv
annotations/session_geometry.csv
```

80 μm ROI 是基于 60–120 μm 候选尺寸的全批次敏感性分析选择的稳定折点，不是 nominal 200 μm 编程区域本身。

该中央 ROI 的目的，是减少弓字形换向、加减速和额外边界扫描对材料主体响应分析的干扰。

---

# 6. Session 几何状态

三个 session 仍应作为不同 measurement session 保留，但方向状态已经确认，不再是“待确认”。

| session | 固定旋转角 | D4 方向状态 |
|---|---:|---|
| `zro2_120_formal` | 约 −0.70° | identity，confirmed |
| `zro2_60_pass` | 约 −0.45° | identity，confirmed |
| `zro2_20_supplement` | 约 −0.65° | identity，confirmed |

这解决的是图像规范方向问题，不代表三个 session 没有装夹、仪器漂移或测量域差异。`session_id` 必须永久保留。

当前仍缺少 scanner 坐标与共聚焦像素坐标之间的独立绝对几何真值，因此只能声称建立了统一、可重复的 nominal process-coordinate registration，不能声称绝对中心误差小于某个未知阈值。

---

# 7. 锥坑修复状态

当前正式 bundle 中：

```text
存在修复像素的样本：52/200
总修复像素：5,593
全部像素：5,120,000
总体修复比例：约 0.109%
```

修复算法只接受紧凑向下异常，并保护边界、拒绝长扫描沟槽。当前正式规则：

```text
height_raw       主证据，不覆盖
height_repaired  辅助敏感性版本
repair_mask      必须随修复数据同时使用
```

不能在 raw 与 repaired 之间根据结果是否“更漂亮”进行选择性报告。

---

# 8. Compressor steps 与真实脉宽

三个矩形设计表中的原字段“脉宽”实际记录：

$$\boxed{\text{compressor steps}}$$

而不是 fs。当前背景资料确认的标定关系为：

| pulse duration / fs | compressor steps |
|---:|---:|
| 223 | 29400 |
| 500 | 36140 |
| 1000 | 43800 |
| 2000 | 58400 |
| 3000 | 72900 |
| 4000 | 87300 |
| 5000 | 101800 |
| 6000 | 116200 |
| 7000 | 130900 |
| 8000 | 145300 |
| 9000 | 159800 |
| 10000 | 173500 |

数据管理必须同时保留：

```text
compressor_steps
pulse_duration_fs
pulse_duration_calibration_id
pulse_duration_calibration_version
```

当前设计 CSV 和高度 bundle 尚未直接携带标定 ID/版本。Phase 1 manifest 应将本说明中的映射登记为独立 calibration provenance，而不是把转换逻辑散落在分析代码中。

---

# 9. 120 正式矩形 DOE

样本数：120。原始字段：

```text
加工顺序
中心X
中心Y
脉宽（实际为 compressor steps）
频率
线间距
次数
速度
```

参数范围：

```text
pulse duration: 500, 1000, 2000, 4000, 6000 fs
frequency:      2, 10, 50, 100, 200 kHz
hatch spacing:  2, 4, 6, 8, 10 μm
pass:           1, 2, 3, 4, 5
velocity:       3, 5, 7, 9, 11 mm/s
```

这是 120 条空间填充型组合，不是 $5^5$ 全因子设计。

完整五维工艺组合共有 119 个不同值。加工顺序 49/50 是唯一完全相同的五维工艺重复：

```text
compressor_steps = 58400
pulse_duration   = 2000 fs
frequency        = 2 kHz
hatch_spacing    = 6 μm
pass             = 2
velocity         = 9 mm/s
```

二者位置不同，必须保留。它们只能作为单对 `repeatability sentinel`，不能估计完整实验噪声分布或模型误差下限。

---

# 10. Pass 数据

## 10.1 N=1–4

60 pass 数据由：

$$15\text{ 个基础工艺条件}\times N=1,2,3,4$$

构成。

参数水平：

```text
pulse duration: 223, 500, 1000, 2000, 4000 fs
frequency:      2, 5, 10, 20, 40 kHz
hatch spacing:  2, 4, 6, 8, 10 μm
velocity:       5, 10, 15, 20, 25 mm/s
pass:           1, 2, 3, 4
```

不同 N 对应不同空间位置分别加工至目标 pass 后测量，不是同一加工面在每一 pass 后反复测量。因此应称为：

$$\boxed{\text{cross-sectional pseudo-temporal trajectories}}$$

而不是真实 longitudinal dynamics。

## 10.2 N=5–6

补充数据由：

$$10\text{ 个基础工艺条件}\times N=5,6$$

构成，并能与 N=1–4 中的 10 个基础条件匹配。

但是：

$$N\ge5\Longleftrightarrow\text{supplement session}.$$

所以 N=4→5 的 pass effect 与 session effect 完全混杂。即使观察到系统性跳跃，也不能识别其来源。当前可靠的主轨迹分析应限于 N=1–4。

---

# 11. 激光功率与光学背景条件

当前实验背景资料记录：

```text
software nominal power:          10 W
measured post-objective power:   5.3333 W
wavelength:                      1030 nm
NA:                              0.45
M²:                              1.2
focus strategy:                  fixed at original top surface
```

按背景资料计算的 nominal 参数：

```text
1/e² waist radius w0 ≈ 0.874 μm
diameter 2w0        ≈ 1.748 μm
Rayleigh length zR  ≈ 1.943 μm
```

这些是 nominal optical parameters，不是槽内局部光场或吸收能量的直接测量。

当前功率数值尚未进入仓库的正式 session manifest，也未附独立测量记录 ID。后续生成 `pulse_energy_uJ` 或 `nominal_energy_dose` 前，应登记：

```text
measured_power_W
power_measurement_source
power_measurement_version/date
constant-power assumption
```

在功率来源完成登记后，可计算 $E_p=P/f$。但由于 P 固定，频率与单脉冲能量完全耦合，当前设计不能独立识别 repetition-rate effect 与 pulse-energy effect。

> **登记状态更新（2026-09-04，外审 §1 更新决定）**：
> 上文"尚未进入正式 manifest / 未附独立测量记录 ID"的表述已被后续进展部分取代——`outputs/phase2/manifest/phase2_manifest.csv` 已登记 `measured_power_W = 5.3333`、`power_measurement_source`、`power_measurement_version`（登记前历史状态标 `PENDING_REGISTRATION`）。经确认该功率为**物镜后独立实测平均功率且物理可信**，已按外审更新决定升级为 **canonical physical input**（原则：measurement is physically trusted ≠ measurement provenance metadata is perfectly complete）。仪器型号/测量日期若无记录，登记 `instrument metadata unavailable`，不因此把测量值降为 proxy。
> Canonical 派生：`pulse_energy_uJ = P_obj/f`、`areal_dose_J_per_mm2 = P_obj·N/(v·h)`，单一事实源见 `src/provenance.py` 的 `POWER_REGISTRY`；Phase 2–2.7 的 `*_proxy` 旧列保留供复现，Phase 2.8 起只用 canonical 列。
> 辨识性提醒（不变）：P 恒定 ⇒ f 与 E_p 完全耦合，正式措辞仍应为 **frequency / pulse-energy coupled effect**。

---

# 12. 扫描方式与稳定 ROI 的含义

矩形区域采用弓字形填充，nominal programmed region 为：

$$200\times200\ \mu m.$$

已知非理想因素包括：

- 长扫描线端部加速/减速；
- 换向区局部实际速度低于命令速度；
- 层间可能出现额外边框或边线扫描；
- 软件缺少关激光空走；
- 焦点固定在原始上表面，不随槽深动态跟焦。

因此边界深沟不能直接解释为材料本征响应。当前 80×80 μm ROI 是中央稳定材料响应窗口，不代表完整 200 μm 编程区域，也不能用于研究边界曝光历史。

---

# 13. 当前高度统计快照

对当前 `height_raw` bundle 的只读检查得到：

```text
global height range:          −78.41 ～ 11.69 μm
sample-wise mean range:       −64.09 ～ −0.16 μm
all bundle pixels finite:     true
```

快速 PCA 压力测试显示：

```text
absolute-height PCA PC1 EVR:             约 98.77%
corr(PC1, sample-wise mean):             约 1.0
sample-mean-removed residual PC1 EVR:    约 27.71%
residual cumulative EVR, first 10 PCs:   约 78.14%
```

这些数字是探索前的快速诊断，不是冻结 Phase 1 结果。正式分析将使用 valid-mask median 定义深度，并重新计算所有指标。

当前诊断已经明确表明：绝对高度的主要变化是样本间垂直偏移/加工深度；不能据此声称二维残差形貌接近一维。

正式 DOE 49/50 的快速参照为：

```text
absolute height RMSE:    约 0.2876 μm
mean-depth difference:   约 0.0167 μm
demeaned-shape RMSE:     约 0.2871 μm
random-pair RMSE median: 约 19.6858 μm
```

该结果只支持“这一特定重复对高度一致”，不支持通用噪声方差估计。

---

# 14. 氧化锆单线数据

设计表：

```text
氧化锆/氧化锆_line_design.csv
```

共有 120 条设计记录，字段中的脉宽已经是真实 fs：

```text
pulse duration: 223, 500, 1000, 2000, 4000 fs
frequency:      2, 5, 10, 20, 40 kHz
pass:           1, 2, 3, 4, 5
velocity:       5, 10, 15, 20, 25 mm/s
```

单线没有 hatch spacing。仓库中存在单线 pilot 分析输出，但当前尚未形成与矩形 200 样本同级、统一契约的完整 120 样本高度数据包。

因此单线数据暂不进入 Phase 1。后续可用于跨几何验证，但不能与矩形面形貌直接视为同一响应对象。

---

# 15. 其他材料

背景资料记录还存在 CFRP、Al/SiC 和 SiC。当前仓库尚未完成这些材料的统一盘点，具体样本数、DOE、测量格式和光学条件不得写死。它们不进入当前 Phase 1。

---

# 16. 当前数据适合支持的问题

当前 200 个矩形稳定 ROI 可以支持：

1. 工艺记录与高度图的一一对应审计；
2. 加工深度与二维残差形貌的分离；
3. absolute/residual PCA 的描述性变异分析；
4. 残差 PCA 子空间的共享测量聚类 bootstrap；
5. raw/repaired 敏感性分析；
6. 正式 DOE 49/50 的单对重复性参照；
7. N=1–4 横截面 pass 轨迹的描述性分析。

---

# 17. 当前数据不能直接支持的结论

当前数据不能直接建立：

- PCA 维数等于物理状态维数；
- predictive sufficient state；
- ML predictability；
- 完整实验噪声分布；
- N=4→5 的机制转变；
- 同一位置真实逐 pass 或逐脉冲 dynamics；
- repetition rate 与 pulse energy 的独立因果效应；
- 槽内真实局部吸收、光场、等离子体或电子动力学；
- 亚表面缺陷、残余应力或相变状态；
- 由宏观形貌完整反演微观激光–材料机制。

---

# 18. 当前数据管理规则

后续统一 manifest 至少保留：

```text
dataset_index
session_id
measurement_id
sample_id
shared_height_source_id
design_group
processing_order
x_position
y_position

compressor_steps
pulse_duration_fs
pulse_duration_calibration_id
frequency_kHz
hatch_spacing_um
pass_count
velocity_mm_s

measured_power_W
power_measurement_source
```

基本规则：

1. 永久保留 raw compressor steps；
2. 标定值与原始记录分列保存；
3. 不按数组行号连接数据；
4. 不把共享高度来源中的 ROI 当作完全独立测量；
5. 所有派生量记录公式、单位、假设和 provenance；
6. raw 高度始终为主证据；
7. session 身份不得因方向已统一而删除。

---

# 19. 当前数据基础一句话总结

目前已经形成：

$$
\boxed{
120\text{ 个正式 DOE ROI}
+15\text{ 条 }N=1\text{–}4\text{ 横截面 pass 轨迹}
+10\text{ 组补充 }N=5,6
+120\text{ 条单线设计记录}
}
$$

其中矩形数据已经通过直接 CAG 解析、measurement 平面校正、session 方向统一、人工冻结注册和中央稳定 ROI 提取，形成 200×160×160 的正式高度数据包。

当前最合理的近期任务不是继续扩展模型，而是：

$$
\boxed{
\text{Manifest integrity}
\rightarrow
\text{Depth–shape separation}
\rightarrow
\text{Residual-mode robustness}
\rightarrow
\text{N=1–4 trajectory description}
}
$$

到此停止，再由结果决定 Phase 2。
