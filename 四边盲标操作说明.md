# 四边盲标操作说明

## 启动

双击 `启动四边盲标_A.bat`。不要同时启动多个标注窗口，也不要用 Excel
打开结果 CSV。程序会自动从第一条未完成记录继续，并跳过已经保存的样本。

## 如何框选

图像已经按固定 session 角度旋正，横轴 `u` 向右，纵轴 `v` 向下。按住鼠标
左键从一个角拖到对角，松开后可拖动红框和控制点继续微调。

边界取“稳定未加工外表面 → 连续加工内部”的过渡带中线：

- 以每条边最长、连续的部分为准；
- 忽略孤立飞溅、颗粒和角部毛刺；
- 连续的凸起/凹陷边带取其中线，不取最外沿；
- 不要强迫宽高等于 200 μm；
- 不要参考任何自动红框或用对边推算看不清的边。

如果任意一条边确实看不清，填写原因并按 `U` 标为 Unusable。

## 快捷键

- `S`：保存当前框并进入下一条；
- `A` / `D`：上一条 / 下一条；
- `Z`：恢复本条已保存的框；
- `R`：清除当前框并重画；
- `C`：循环切换局部对比度；
- `H`：切换高度/深度显示；
- `U`：本条无法可靠标注，并进入下一条。

框选后底部会实时显示宽×高。每次按 `S` 或 `U` 都会原子写入
`outputs/rectangle_registration/registration/manual_four_edge_validation.csv`，
中途关闭后可以继续。
