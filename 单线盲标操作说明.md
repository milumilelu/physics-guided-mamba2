# 单线加工范围盲标操作说明

## 适用数据

`氧化锆/120组直线.cag` 的 120 条单线（DOE 设计表 `氧化锆/氧化锆_line_design.csv`，
加工顺序 = CAG Path，沿用单线 pilot 的用户确认映射）。每条单线是一次窄条带
测量，需要人工用**狭长矩形**标出加工范围，作为自动线检测之外的人工基准。

## 首次使用前：生成标注表

```bat
.venv\Scripts\python.exe scripts\33_build_single_line_annotation_table.py --annotator A
```

构建器会对每组做鲁棒参考平面拟合（沿用 pilot 冻结约定：非对称 ±2.5σ/4σ 截断，
depth = plane − z），估计线的深度加权方向角并冻结视图清单
`annotations/single_line_view_manifest.csv`，同时生成空标注表
`annotations/single_line_range_annotation.csv`。

- 方向角只用于把显示视图旋正（单线横平），**不作为任何边界信息展示**；
- 构建结束会打印每组 `theta / signal px`，信号像素过少的组会提示 REVIEW；
- 可选 `--render-preview N` 输出前 N 组的旋正预览图到
  `outputs/single_line_annotation_preview/` 供人工核对；
- 如需重建：已标注时必须加 `--force`，已有标注值会按 measurement_id 保留。

## 启动标注

双击 `启动单线盲标_A.bat`（表不存在时会自动先跑构建器）。不要同时开多个
标注窗口，也不要在标注时用 Excel 打开结果 CSV。程序自动从第一条未完成
记录继续。

## 如何框选（狭长矩形）

图像已按每组方向角旋正：横轴 `u` 沿加工方向，纵轴 `v` 向下。按住左键从
一角拖到对角，松开后可拖动红框和控制点微调。

- **长轴沿加工方向**：两个短边 = 单线的起止端点（加工范围）；
- 两个长边 = 单线两侧边界：取"稳定未加工参考面 → 连续加工内部"过渡带中线；
- 端点取连续加工痕迹的终止处，忽略端部孤立的飞溅、球形颗粒与碎屑；
- 连续的凸起/凹陷边带取中线，不取最外沿；
- 状态栏实时显示 长 × 宽 和长宽比；长宽比 < 3 时程序会提醒（仍可保存）；
- 不要参考任何自动检测结果，也不要用理论线长 200 μm 反推端点。

如果整条线的范围确实看不清，按 `U` 标为 Unusable（会写入默认备注）。

## 快捷键

- `S`：保存当前框并进入下一条；
- `A` / `D`：上一条 / 下一条；
- `Z`：恢复本条已保存的框；
- `R`：清除当前框并重画；
- `C`：循环切换局部对比度；
- `H`：切换高度/深度显示；
- `U`：本条无法可靠标注，并进入下一条。

## 数据文件

| 文件 | 说明 |
| --- | --- |
| `annotations/single_line_view_manifest.csv` | 每组的平面系数、方向角、裁剪参数（构建器生成） |
| `annotations/single_line_range_annotation.csv` | 可继续编辑的人工标注表 |
| `annotations/single_line_annotation_draft_<a>.json` | 未保存框的崩溃恢复草稿，保存成功后自动覆盖 |

每次按 `S` 或 `U` 都会原子写入标注表，中途关闭后可以继续。全部标注完成并
复核后，再复制一份冻结副本（参照四边流程 `*_frozen.csv` 的做法），
不要让下游直接改写人工标注原件。
